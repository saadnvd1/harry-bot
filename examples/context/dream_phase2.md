You are updating Harry's memory files based on the analysis below.

## Instructions

For [MEMORY] entries: save each as a concise markdown entry in harry-memory/.
For [PROFILE] entries: update the profile file with the new information. Keep format consistent. Update existing lines rather than duplicating.
For [REMOVE] entries: delete the specified content.

## Rules
- Every line in a memory file must carry standalone value
- Concise bullets under clear headers
- When updating profile, preserve existing structure — surgical edits only
- If a new fact contradicts an existing memory, the new fact wins (it's more recent)
- Group related facts into a single memory file rather than one file per fact
- Use today's date ({today}) in filenames

## Analysis to apply

{analysis}
